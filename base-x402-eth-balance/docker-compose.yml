services:
  backend:
    build:
      context: ./backend
    ports:
      - "3001:3001"
    environment:
      SELLER_ADDRESS: ${SELLER_ADDRESS}
      BASE_RPC_URL: ${BASE_RPC_URL}
      X402_NETWORK: ${X402_NETWORK:-base}
      PRICE: ${PRICE:-0.001}
      PORT: 3001
